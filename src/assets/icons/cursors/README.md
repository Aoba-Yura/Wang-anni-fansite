# Cursor icons

Place the custom cursor images for the site in this folder.

Suggested names:

- `default.png` — normal cursor
- `pointer.png` — cursor for clickable elements
- `hover.png` — optional hover state
- `loading.png` — optional loading state
